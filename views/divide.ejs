<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <title><%=pageTitle%></title>
</head>
<body>
    <header class="navbar navbar-expand-lg bd-navbar sticky-top>
    <nav class="navbar bg-body-tertiary fixed-top" style="background-color: #95cbf1;" data-bs-theme="light">
  <div class="container-fluid">
   <span class="navbar-text">Test Your Skills</span>
    <a class="navbar-brand">Division Quiz</a>
    <a href="/" class="btn btn-outline-secondary">
    <i class="bi bi-house"></i>
    </a>
  </div>
</nav>
</header>
    <div class="container mt-5 pt-5">
    <% if (num1 && num2) { %>
        <h2 class="text-center mb-4">
            Question <%= questionNumber %> of <%= totalQuestions %>
        </h2>
        <h2 class="text-center mb-4">Solve:</h2>
    <div class="row justify-content-center align-items-center">
            <div class="col-4 text-end">
                <h1 class="display-4">
                    <%= product %> &divide; <%= num1 %> = 
                </h1>
            </div>
            <div class="col-4">
                <% if (!message) { %>
                <form action="/divide/check-answer" method="POST" novalidate>
    <input 
        type="text" 
        inputmode="numeric"
        pattern="[0-9]*"
        class="form-control form-control-lg mx-auto"
        style="width: 100px; height: 100px; font-size: 2rem; text-align: center;"
        id="answer" name="answer" value="<%=answer%>" required autofocus autocomplete="off">
    <input type="hidden" name="num1" value="<%= num1 %>">
    <input type="hidden" name="num2" value="<%= num2 %>">
    <input type="hidden" name="product" value="<%= product %>">
</form>
<%} else {%>
    <h1 class="display-4">
                    <%= answer %>
                </h1>
    <% } %>
            </div>
        </div>
        <div class="row">
            <div class="col text-center">
                <% if (message) { %>
                    <p style="color: <%= message.startsWith('Correct') ? 'green' : 'red' %>;">
                        <%= message %>
                    </p>
                <% } %>
                <% if (message) { %>
                    <form action="/divide" method="GET" style="margin-top:20px;">
                        <input type="hidden" name="next" value="true">
                        <button type="submit" class="btn btn-primary">Next</button>
                    </form>
                <% } %>
            </div>
        </div>
    <% } else { %>
        <h2 class="text-center mb-4">
            <%= message %>
        </h2>
        <form action="/divide" method="GET">
            <button type="submit" class="btn btn-primary">Restart</button>
        </form>
    <% } %>
</div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.querySelector('form').addEventListener('submit', function(event) {
            event.preventDefault();
            this.submit();
        });
    </script>
</body>
</html>